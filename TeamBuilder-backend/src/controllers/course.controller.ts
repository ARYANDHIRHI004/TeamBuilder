import asyncHandler from "../utils/asyncHandler";
import type { Request, Response, NextFunction } from "express";


export const createCourse = asyncHandler(async (req:Request, res:Response, nest:NextFunction) => {
    // const {} = req.body;

})